# Backend configuration, read from the process environment at startup.
# (No .env auto-loading yet — set these when launching uvicorn if needed;
# the defaults cover local development.)

# Comma-separated origins allowed to read the API (CORS allowlist).
# MIRROR_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
