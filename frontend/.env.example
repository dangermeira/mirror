# Copy to .env and adjust if your backend runs elsewhere.
# NOTE: VITE_* values are substituted into the public JavaScript at build
# time and ship to every visitor — configuration only, never secrets.
VITE_API_BASE_URL=http://localhost:8000
